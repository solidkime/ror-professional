/ table.table
/   thead
/     tr
/       th Body
/   tbody
/     - @question.answers.each do |answer|
/       tr
/         td=  answer.body
/         - if user_signed_in? && current_user.author_of?(answer) && !answer.new_record?
/             td= link_to "Destroy answer", answer_path(answer), method: :delete